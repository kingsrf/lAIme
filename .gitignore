# .gitignore for Node.js Project with Frontend UI

# === Dependencies ===
# Ignore the node_modules directory, which holds installed packages.
/node_modules/

# === Secrets & Environment Variables ===
# Ignore environment files containing sensitive API keys, passwords, etc.
# Use '.env.example' or '.env.template' (which SHOULD be committed) for structure.
.env
.env.*
# DO NOT ignore example environment files
!/.env.example
!/.env.template

# === Logs ===
# Ignore log files generated during runtime or debugging.
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# === Build & Output ===
# Ignore compiled files, distribution folders, or build output.
# This commonly includes bundled/minified CSS, JS, and copied HTML assets.
/build/
/dist/
.out/
# Framework-specific build outputs
/.next/
/.nuxt/
/.svelte-kit/
# Add others if you use a different framework or build tool

# === Frontend Specific ===
# Ignore caches generated by CSS preprocessors like Sass.
.sass-cache/

# NOTE: Source files like *.html, *.css, *.js, *.jsx, *.ts, *.tsx, *.vue, *.svelte
# are generally NOT ignored as they are the core of your UI code.
# Only ignore specific generated files if necessary (e.g., large generated data JSONs).

# === Test Output ===
# Ignore test reports and coverage data.
coverage/
.nyc_output/

# === Runtime Data ===
# Ignore runtime data files generated by package managers or tools.
.npm/
.yarn/
# package-lock.json, yarn.lock, pnpm-lock.yaml are typically COMMITTED for applications

# === Operating System Files ===
# Ignore OS-specific hidden or metadata files.

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
__MACOSX

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Linux
*~

# === Editor & IDE Configurations ===
# Ignore configuration files specific to certain editors or IDEs.

# VSCode
.vscode/*
# Allow specific shared settings if needed (uncomment and adjust)
# !.vscode/settings.json
# !.vscode/tasks.json
# !.vscode/launch.json
# !.vscode/extensions.json
*.code-workspace

# JetBrains (WebStorm, IntelliJ IDEA, etc.)
.idea/

# Sublime Text
*.sublime-project
*.sublime-workspace

# Atom
.atom/

# Vim
*.swp
*.swo

# Emacs
*#
#*#

# === Miscellaneous ===
# Ignore other common temporary or generated files.
.eslintcache
*.gz